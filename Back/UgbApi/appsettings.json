{
  "ConnectionStrings": {
    "DbConnectionString": "Server=URIEL-MOUTINHO\\SQLEXPRESS; Database=DbUgb; Trusted_Connection=True; TrustServerCertificate=Yes"
    //Aqui em si é a connection string, você deve alterar para colocar a string de conexão com seu banco, altere a conexão e rode dois comandos (1º dotnet ef migrations add 'initial' --context 'UgbDbContext' 
    //2º dotnet ef databaseupdate) com esses dois comandos se gera a migração inicial e o segundo cria a tabela no banco ou a atualiza caso esteja criada a base utilizada é o SQL (EXPRESS).
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
